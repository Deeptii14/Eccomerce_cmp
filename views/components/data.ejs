<!DOCTYPE html>
<html lang="en">

<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Details</title>
     <style>
          .detail {
               display: flex;

               flex-direction: column;
               align-items: center;
               margin-top: 58px;
               gap: 14px;
               border: 2px solid whitesmoke;
               padding: 8px;
               height: 100vh;
          }

          img:hover {
               transform: scale(1.2);
               transition: all 1.09s;
          }
     </style>
</head>

<body>
     <div class="detail">
          <h1>Details of User Logged In</h1>
          <img src=<%=user.profilepic %> height="200px" width="200px" alt="photo missing">
          <h2>Email : <%= user.email %>
          </h2>
          <h2>Name : <%= user.name %>
          </h2>
          <% if (user.isAdmin) { %>
               <p style="color: red ;font-size: larger; font-weight: 800;">
                    Role :Admin
               </p>
               <% } %>
     </div>
</body>

</html>